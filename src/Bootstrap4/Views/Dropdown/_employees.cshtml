@model IEnumerable<Bootstrap4.Models.EmployeeModel>

<div id="employees" class="dropdown-menu">
    @foreach (var employee in Model)
    {
        <a class="dropdown-item" href="#" data-id="@employee.Id">
            @employee.Name
        </a>
    }
</div>